# utils/TEMPLATE/Makefile - Template for new utilities
# Copy this file to your new utility directory and customize as needed

include ../../config.mk

#------------------------------------------------------------------------------
# Configuration Overrides (uncomment to customize)
#------------------------------------------------------------------------------
# FILENAME := CUSTOM.ODY            # Override auto-generated name
# C_SOURCES := main.c helper.c      # Explicitly list C files (default: all .c)
# ASM_SOURCES := startup.asm        # Explicitly list ASM files (default: all .asm)
# C_FLAGS += -DDEBUG                # Add additional C compiler flags
# C_INCLUDES += -I../../somelib     # Add additional include directories
# MEM_TARGET := ext-d,ext-e,ext-de  # Override default memory target

INSTALL_SUBDIR := SYS

include ../../rules.mk
